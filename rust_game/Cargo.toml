[package]
name = "purridor"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]
test = false
bench = false
doctest = false

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
libm = "0.2"